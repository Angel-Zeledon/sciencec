# UI test: a numeric literal followed by something that is not a suffix
# (SC0009).
#
# §4.1 gives the suffixes: `42i32`, `7u8`, `2.5f32`. The lexer consumes
# anything word-shaped after a literal so that `42q` stays one broken literal
# instead of becoming an integer next to an identifier `q`.

fn main():
    let counted = 42q
    let ratio = 2.5i32
