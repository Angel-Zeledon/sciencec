# UI test: a tab in the indentation is a lexical error (SC0003).
#
# Spec §4.2: "Spaces only. A tab in the indentation is an error (`SC0003`),
# with no attempt to interpret it."

def main():
	print("indented with a tab")
