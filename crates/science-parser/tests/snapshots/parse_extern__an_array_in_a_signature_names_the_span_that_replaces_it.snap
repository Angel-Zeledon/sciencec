---
source: crates/science-parser/tests/parse_extern.rs
expression: "parse_source_allowing_errors(r#\"unsafe extern \"C\" library \"openblas\":\n    def takes(a: &Array[F64], b: &mut Array[F64], c: Array[I32])\n\"#)"
---
Module @0..103
  Extern "C" unsafe @0..102
    Library "openblas" @18..36
    items
      ExternFn `takes` @42..102
        params
          Param `a` @52..66
            type: Borrowed @55..66
              Path `Array` @56..66
                generics of `Array`
                  Path `F64` @62..65
          Param `b` @68..86
            type: Borrowed mutable @71..86
              Path `Array` @76..86
                generics of `Array`
                  Path `F64` @82..85
          Param `c` @88..101
            type: Path `Array` @91..101
              generics of `Array`
                Path `I32` @97..100
--- diagnostics ---
SC0421 @55..66 `Array[T]` has no C representation
  note the length does not cross the ABI — it exists on the Science side so that the wrapper's bounds check is an expression the compiler checks
  note a call site still passes the array: `&Array[T]` coerces to `ffi.Span[T]` there, and `.span()` names the conversion where it has to be written out
SC0421 @71..86 `Array[T]` has no C representation
  note the length does not cross the ABI — it exists on the Science side so that the wrapper's bounds check is an expression the compiler checks
  note a call site still passes the array: `&Array[T]` coerces to `ffi.Span[T]` there, and `.span()` names the conversion where it has to be written out
SC0421 @91..101 `Array[T]` has no C representation
  note the length does not cross the ABI — it exists on the Science side so that the wrapper's bounds check is an expression the compiler checks
  note a call site still passes the array: `&Array[T]` coerces to `ffi.Span[T]` there, and `.span()` names the conversion where it has to be written out
