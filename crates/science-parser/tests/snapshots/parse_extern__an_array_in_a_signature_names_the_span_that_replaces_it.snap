---
source: crates/science-parser/tests/parse_extern.rs
expression: "parse_source_allowing_errors(r#\"unsafe extern \"C\" library \"openblas\":\n    def takes(a: borrowed Array of F64, b: mutable borrowed Array of F64, c: Array of I32)\n\"#)"
---
Module @0..129
  Extern "C" unsafe @0..128
    Library "openblas" @18..36
    items
      ExternFn `takes` @42..128
        params
          Param `a` @52..76
            type: Borrowed @55..76
              Path `Array` @64..76
                generics of `Array`
                  Path `F64` @73..76
          Param `b` @78..110
            type: Borrowed mutable @81..110
              Path `Array` @98..110
                generics of `Array`
                  Path `F64` @107..110
          Param `c` @112..127
            type: Path `Array` @115..127
              generics of `Array`
                Path `I32` @124..127
--- diagnostics ---
SC0421 @55..76 `Array of T` has no C representation
  note the length does not cross the ABI — it exists on the Science side so that the wrapper's bounds check is an expression the compiler checks
  note a call site still passes the array: `borrowed Array of T` coerces to `ffi.Span of T` there, and `.span()` names the conversion where it has to be written out
SC0421 @81..110 `Array of T` has no C representation
  note the length does not cross the ABI — it exists on the Science side so that the wrapper's bounds check is an expression the compiler checks
  note a call site still passes the array: `borrowed Array of T` coerces to `ffi.Span of T` there, and `.span()` names the conversion where it has to be written out
SC0421 @115..127 `Array of T` has no C representation
  note the length does not cross the ABI — it exists on the Science side so that the wrapper's bounds check is an expression the compiler checks
  note a call site still passes the array: `borrowed Array of T` coerces to `ffi.Span of T` there, and `.span()` names the conversion where it has to be written out
