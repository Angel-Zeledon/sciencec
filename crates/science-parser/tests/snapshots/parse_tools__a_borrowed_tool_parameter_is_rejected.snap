---
source: crates/science-parser/tests/parse_tools.rs
expression: "parse_source_allowing_errors(\"## Count the rows of a table.\ntool count_rows(rows: &Array[Int], scratch: &mut Int) -> Int:\n    0\n\")"
---
Module @30..98
  Tool `count_rows` @30..97
    params
      Param `rows` @46..63
        type: Borrowed @52..63
          Path `Array` @53..63
            generics of `Array`
              Path `Int` @59..62
      Param `scratch` @65..82
        type: Borrowed mutable @74..82
          Path `Int` @79..82
    ret: Path `Int` @87..90
    body: Block @96..97
      tail: Int 0 @96..97
--- diagnostics ---
SC0192 @52..53 a `tool` owns its arguments
  note a tool's arguments are built from what the caller sent, so the tool is the only owner there is
SC0192 @74..78 a `tool` owns its arguments
  note a tool's arguments are built from what the caller sent, so the tool is the only owner there is
