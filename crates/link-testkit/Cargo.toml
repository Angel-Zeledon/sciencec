[package]
name = "link-testkit"
version.workspace = true
edition.workspace = true
license.workspace = true

# No dependencies, and none in the tests either. The harness takes the compile
# step as a closure precisely so that it does not depend on any phase of the
# compiler: it has to be finished and trustworthy before the lexer, the parser
# or the type checker are.
[dependencies]

[dev-dependencies]
